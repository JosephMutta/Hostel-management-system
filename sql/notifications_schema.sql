CREATE TABLE hostel_owner_notifications (id INT AUTO_INCREMENT PRIMARY KEY, owner_id INT NOT NULL, message TEXT NOT NULL, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP, read_status BOOLEAN DEFAULT FALSE, FOREIGN KEY (owner_id) REFERENCES users(id) ON DELETE CASCADE);
